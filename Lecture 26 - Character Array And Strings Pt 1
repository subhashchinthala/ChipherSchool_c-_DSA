// Character Array And Strings Pt 1

#include<iostream>
using namespace std;

int main()
{
    /*//char s,u,d,h,a;

    //cin>>s>>u>>d>>h>>a;

    char name[1000];

    cout<<"Enter your name : (Note: Enter # after the end of your name: )"

    int i=0;   // Initial value of i is  0

    while(1)  // Here i is the previous input
    {
        cin>>name[i];

        if(name[i]=='#') break;

        i++;   // When the name is begin entered, the value of i increasing 
    }

    i=0;  // Again need to  change the value of i to 0 before ruuning the second while loop

    

    cout<<"The name you have just entered is "<<endl;

    while(name[i]!='#')  // We have again used i
    {
        cout<<name[i];
        i++;
        
    }*/

   string name; // Over here string is a new datatype and not a name so string is similat=r to int, float, double

   cin>>name;
   
   cout<<name;

    
}
